diff --git a/bin.cjs b/bin.cjs
index 07274ea6f53fd54a129b3d338892feda6e995cff..9cb57ac8bb9b59e471d07d854dcf4f2023650e84 100755
--- a/bin.cjs
+++ b/bin.cjs
@@ -1463,6 +1463,7 @@
           terminal.clear();
           return result;
         } catch (err2) {
+          console.error(err2);
           terminal.reject(err2);
           process.exit(1);
         }
@@ -17856,8 +17857,11 @@
               progressCallback("checks", checksCount, "fetching");
             }
             for (const checks of tableChecks) {
-              let checkValue = checks.constraint_definition;
-              const constraintName = checks.constraint_name;
+              let checkValue =
+                checks.constraint_definition ?? checks.CONSTRAINT_DEFINITION;
+              const constraintName =
+                checks.constraint_name ?? checks.CONSTRAINT_NAME;
+              if (typeof checkValue !== "string" || !constraintName) continue;
               checkValue = checkValue.replace(/^CHECK\s*\(\(/, "").replace(/\)\)\s*$/, "");
               checkConstraints[constraintName] = {
                 name: constraintName,
